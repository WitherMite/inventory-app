<div class="shop-interface">
  <search>
    <form action="/" class="item-search">
      <input type="search" name="q" id="item-search" />
      <button type="submit">Search</button>
    </form>
  </search>
  <div class="categories">
    <% categories.forEach((category) => { %>
    <a href="/category?q=<%=category.id%>" class="category-link">
      <%=category.name%>
    </a>
    <%});%>
  </div>
  <div class="items">
    <!-- generated list of items that fit search query-->
    <% items.forEach((item) => { %>
    <a href="/item?q=<%=item.id%>" class="item-link">
      <div class="item-card">
        <h2><%=item.name%></h2>
        <p><%=item.price%></p>
      </div>
    </a>
    <%});%>
  </div>
</div>
